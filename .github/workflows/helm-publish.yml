name: Publish

on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - project/*
    tags:
      - "v*.*.*"

permissions:
  contents: read
  packages: write
  pull-requests: read

jobs:
  _:
    uses: toggle-corp/banjo-action/.github/workflows/pipeline.yml@v0.4.0
    with:
      # -- Same as CI (./ci.yml)
      # Job: Pre-commit
      pre_commit__apt_packages: "gdal-bin libgdal-dev"
      # Job: Docker
      docker__wait_cmd: "./manage.py wait_for_resources --db --cache --celery-broker"
      docker__migration_dummy_test: "main.tests.fake_test.FakeTest"
      docker__run_graphql_check: true
      docker__compose_file: "gh-docker-compose.yml"
      docker__compose_service_name: "web"
      docker__compose_test_cmd: "/code/misc/run_tests.sh"
      # Job: Helm
      helm__chart_directory: "./helm"
      # Pinned: azure/setup-helm defaults to "latest", and Helm patch releases shift
      # rendered whitespace, which breaks the snapshot test. Bump deliberately,
      # regenerating helm/snapshots/ with the new version in the same commit.
      helm__version: "v4.2.4"
      # -- Publish
      docker__push: true
      helm__push: true
      github_release__create: ${{ github.ref_type == 'tag' }}
